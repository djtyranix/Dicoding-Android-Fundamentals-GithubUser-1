<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/card_view"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:layout_marginLeft="8dp"
    android:layout_marginTop="4dp"
    android:layout_marginRight="8dp"
    android:layout_marginBottom="4dp"
    card_view:cardCornerRadius="4dp" >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:padding="16dp" >

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/img_user_photo"
            style="@style/ImageList"/>

        <TextView
            android:id="@+id/tv_user_name"
            style="@style/TextList.Title"
            android:layout_toEndOf="@id/img_user_photo"
            tools:text="@string/user_name_dummy" />

        <TextView
            android:id="@+id/tv_user_username"
            style="@style/TextList.Subtitle"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_user_name"
            android:layout_toEndOf="@id/img_user_photo"
            tools:text="@string/user_username_dummy" />

        <TextView
            android:id="@+id/tv_user_company"
            style="@style/TextList.Subtitle"
            android:layout_marginTop="4dp"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_user_username"
            android:layout_toEndOf="@id/img_user_photo"
            tools:text="@string/user_company_dummy" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>